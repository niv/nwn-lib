#!/usr/bin/env ruby

script = ARGV.shift

if script.nil? || script.empty?
  $stderr.puts "Syntax: nwn-gff-dsl <script to run> <file(s) to operate on ..>"
  exit 1
end
ARGV.each {|file|
  system("nwn-gff-convert", "-v", "-r", script, "-i", file, "-o", file, "-lauto", "-kin")
}
